import { defineConfig } from 'dumi';

export default defineConfig({
  outputPath: 'dist',
  base: 'nova-kit-js/',
  publicPath: '/nova-kit-js//',
  themeConfig: {
    name: 'novaKit',
  },
  exportStatic: {},
});
